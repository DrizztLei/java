import java.util.Scanner;

public class brac
{

	public static void main(String[] args)
	{
		Scanner scanner = new Scanner(System.in);
		int n = scanner.nextInt();

		int[][] matrix = new int[n][n];

		long count = 0;

		for (int a = 1; a <= n; a++)
		{
			for (int b = 1; b <= n; b++)
			{
				if (a == 1)
				{
					count += n;
				}

				int prepareC = factoryA(a, n);
				int prepareD = factoryB(b, n);

			}
		}
	}

	private static int factoryB(int b, int n)
	{
		return 0;
	}

	private static int factoryA(int a, int n)
	{
		
		return 0;
	}
}
