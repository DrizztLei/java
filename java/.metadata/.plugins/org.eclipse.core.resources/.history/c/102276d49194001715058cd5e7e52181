import java.util.Scanner;

public class brac
{

	public static void main(String[] args)
	{
		Scanner scanner = new Scanner(System.in);
		int n = scanner.nextInt();

		int[][] matrix = new int[n][n];

		long count = 0;

		for (int a = 1; a <= n; a++)
		{
			for (int b = 1; b <= n; b++)
			{
				if (a == 1)
				{
					count += n;
				}

				int prepareC = factory(a, n);
				int prepareD = factory(b, n);

			}
		}
	}

	private static int factory(int a, int n)
	{
		
		return 0;
	}
}
